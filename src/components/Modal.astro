---
const { id = 'modal', title = '' } = Astro.props
---

<!-- Modal component was removed; render contents inline as a simple container -->
<div id={id} class="modal-static">
  {title && <h3 class="text-lg font-semibold mb-2">{title}</h3>}
  <div class="mt-4">
    <slot />
  </div>
</div>
